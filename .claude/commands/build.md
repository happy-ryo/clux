Build the clux workspace and report results.

Steps:
1. Run `cargo build` for debug build
2. If that succeeds, optionally run `cargo build --release` if the user requests it
3. Report build success/failure with any errors
